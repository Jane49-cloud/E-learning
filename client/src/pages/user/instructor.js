import React from "react";
import ProtectedRoutes from "../protected.routes";

const Instructor = () => {
  return <ProtectedRoutes>Become an Instructor</ProtectedRoutes>;
};

export default Instructor;
